// Tailwind CSS configuration (v4)
// Switch dark mode strategy to class so `.dark` on <html> controls dark variants

/** @type {import('tailwindcss').Config} */
const config = {
  darkMode: "class",
};

export default config;
